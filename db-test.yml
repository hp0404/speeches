# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: x230
      POSTGRES_PASSWORD: local_development
    ports:
      - 5432:5432
